workflows:
  ios-native-quick-start:
    name: iOS Native
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: 'Regrowth Clock'
    environment:
      ios_signing:
        # Manual signing: certificate and provisioning profile are uploaded by hand
        # in Codemagic UI (Team settings > codemagic.yaml settings > Code signing
        # identities) under the reference name below — NOT fetched automatically.
        provisioning_profiles:
          - 'Regrowth Clock'
        certificates:
          - 'Regrowth Clock'
      vars:
        BUNDLE_ID: "com.tobiaslauridsen.regrowthclock"
        XCODE_WORKSPACE: "Regrowth Clock.xcodeproj"
        XCODE_SCHEME: "Regrowth Clock"
      xcode: latest
    scripts:
      - name: Set up provisioning profiles settings on Xcode project
        script: xcode-project use-profiles
      - name: Build ipa for distribution
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: false
